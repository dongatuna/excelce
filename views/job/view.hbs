<div class="container">

    <div class="page-header">
        <h1>Title: {{posting.title}}</h1>
        <p class="lead"> </p>
    </div>

    <p> {{posting.description}}  </p>

    <p>By: <strong>{{user.username}}</strong></p>

  <h3>Certifications</h3>
    <hr>
  <div class="row">
        <ul>
            {{#each posting.requirements}}
                <li>{{this}}</li>
            {{/each}}
        </ul>
    </div>
    <hr>

    <!--{{#if update}}
    <h3>{{contacts}} Respondents</h3>
        {{#each posting.respondents}}
            <strong>{{this.respondents.username}}</strong>
            <p>{{this.description}}</p>
            {{# each this.certifications}}
                <ul display="inline">
                    <li >{{this}}</li>
                </ul>
            {{/each}}
        {{/each}}
    {{/if}}
-->
    <hr>


    <i class="fa fa-download" aria-hidden="true"></i> Download Job Flyer <br><br>
    {{#if update}}
        <button type="submit" class="btn btn-primary">Continue</button>
        <a href="/job/update/{{posting._id}}">Edit Job Post</a>
    {{else}}
        <button type="submit" class="btn btn-success">Contact Employer</button>
    {{/if}}


    <hr>

</div>